// API

    // Public
    GET     /api/health
    POST    /api/register
    POST    /api/auth/signin

    // Authentication & Authorization
    GET     /api/users                                  // Just master
    GET     /api/users/:userId                          // Just master
    PATH    /api/users/:userId                          // Just master and itself
    DELETE  /api/users/:userId                          // Just master and itself

    GET     /api/users/:userId/whishlist                // Just master and itself
    POST    /api/users/:userId/whishlist/:productId     // Checks if the product exists and the list already has

// Structure

    // Injection
    // Queries
    // Table structure
    // Remove pseud ACID
